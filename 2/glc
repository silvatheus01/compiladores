A -> id { Print( id ); } = E { Print( "="); }
E -> T E'
E' -> + T { Print( "+"); } E'
    | - T { Print( "-"); } E'
    | ε
T -> F T'
T' -> * F { Print( "*"); } T'
    | / F { Print( "/"); } T'
    | ε
F -> id { Print(id + "@"); }
   | num { Print( num ); }
   | ( E )

Nome das funções, a função print é diferente
N -> dtos( EV )
    | max( EV )
    | print E

Rotina
R -> N( EV )
    
Virgula
V -> ,EV
    | ε

Bloco de código
B -> E;B
    | ε